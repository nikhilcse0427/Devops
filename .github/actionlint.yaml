# Tells actionlint which repository secrets this workflow uses (local lint only).
config:
  secrets:
    - DOCKERHUB_USERNAME
    - DOCKERHUB_PASSWORD
    - RENDER_DEPLOY_HOOK
    - RENDER_DEPLOY_HOOK_URL
