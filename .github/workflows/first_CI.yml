name: first_CI

# Learning workflow — run manually only so pushes trigger second_CI once.
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "code checkout"
        uses: actions/checkout@v4

      - name: run a single script
        run: echo "first CI started."
